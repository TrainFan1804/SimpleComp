package src.parser;

/**
 * The main class for the parser. This is the class that run the parser. 
 * 
 * @author                              o.le
 * @version                             0.4
 * @since                               0.10
 */
public class SimpleParser {

    /**
     * Private constructor of the parser because there is no need for 
     * a parser object.
     */
    private SimpleParser() {}
}
